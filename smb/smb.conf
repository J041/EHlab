[global]
   workgroup = WORKGROUP
   server string = Training File Server
   map to guest = Bad User
   security = user
   log file = /dev/stdout
   logging = file

[public]
   path = /srv/samba/public
   browsable = yes
   writable = no
   guest ok = yes
   read only = yes
